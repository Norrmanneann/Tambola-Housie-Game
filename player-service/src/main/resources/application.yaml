server:
  port: 8080

spring:
  application:
    name: player-service

  datasource:
    url: jdbc:mysql://localhost:3306/player_db
    username: root
    password: Shyam*123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka