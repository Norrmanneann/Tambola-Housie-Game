server:
  port: 8083

spring:
  application:
    name: game-service

  datasource:
    url: jdbc:mysql://localhost:3306/game_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Shyam*123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka